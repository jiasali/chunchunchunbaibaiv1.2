# 端口，默认 3000
PORT=3000

# VAPID 密钥（必须配置，运行 npm run generate-vapid 生成）
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# 可选：VAPID 主题，用于 web-push
VAPID_SUBJECT=mailto:admin@example.com

# 可选：游戏页面来源，用于 CORS
GAME_ORIGIN=*

# 可选：数据存储目录
# DATA_DIR=/var/lib/tabula-push/data
